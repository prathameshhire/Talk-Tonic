import { Space_Grotesk, Monoton, Tourney } from 'next/font/google';

export const spaceGrotesk = Space_Grotesk({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const monoTon = Monoton({
  weight: ['400'],
  subsets: ['latin'],
});

export const tourney = Tourney({
  weight: ['400', '700', '900'],
  subsets: ['latin'],
});
